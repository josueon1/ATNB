# TESTING

**Date:** [2026-05-12]

## Frameworks & Tools
- No automated testing framework (like `pytest` or `unittest`) is currently configured in the repository.

## Structure
- There are no dedicated test directories or files (e.g., `tests/` or `test_*.py`).

## Mocking
- N/A

## Coverage
- N/A (0% automated test coverage).

## Manual Verification
- Testing is currently done manually by running the pipeline (`python -m src.pipeline.pipeline`) and observing the console output logs.
- Visual inspection of the Streamlit dashboard (`streamlit run app/dashboard.py`) is used to confirm the data is rendered correctly.
